# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/

# Environment variables
.env*.local
.env
.env.*
!.env.example

# Credentials — NEVER commit (Telegram session = full account access)
*.session
*.session-journal
nexus_backup_*.tar.gz
.backup_stage_*/
# Telethon session bootstrap scripts (embed StringSession / api_id / api_hash)
auth_session.py
create_session.py
test_signal.py

# Scratch / planning notes (local only)
FIX_PLAN.md
compact.txt
journal/

# Backtest result artifacts (runtime output, regenerable)
backtest_results/
# Backtest kline cache (market data, regenerable via prefetch_data.py)
fusionnew/backtest/cache/
fusionnew/backtest/sweep_ablation_result.json

# Paper/live trading databases (runtime state)
*.sqlite
*.sqlite-shm
*.sqlite-wal

# Common ignores
node_modules
.next/
.DS_Store

# Data & runtime (CRITICAL: prevents disk-full recurrence)
data/
runtime/
*.parquet
*.jsonl
latest_scan_*.json

# Python
__pycache__/
*.py[cod]
*.so
.Python
*.egg-info/
dist/
build/
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Logs
*.log
logs/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~